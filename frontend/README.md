# RunLLM Frontend

SvelteKit + Svelte 5 + Tailwind. Deployed on Vercel.

## Setup

```bash
cd frontend
npm install
cp .env.example .env  # then fill in real values
npm run dev
```

## Env vars

- `PUBLIC_SUPABASE_URL`
- `PUBLIC_SUPABASE_ANON_KEY`
- `PUBLIC_API_BASE_URL`


